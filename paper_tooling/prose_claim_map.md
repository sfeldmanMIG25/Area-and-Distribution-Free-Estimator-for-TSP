# Prose claim -> authority map

Generated by `paper_tooling/build_prose_claim_map.py` from `paper_tooling/prose_claims.py`. Do not edit this file; edit the data module and re-render.

Companion to `paper_tooling/prose_claim_audit.md`. The audit records what each number *is*; this records where it *should come from*. The audit's values expire at the next model swap. This mapping does not.

## Buckets

`bucket` answers one question: where does the authority for this number live?

| bucket | meaning | count |
|---|---|---|
| **BANKED** | a key exists in `paper_tooling/tables/paper_numbers.json` | 71 |
| **GENERATED** | a script writes it to disk, but not into the number bank | 13 |
| **UNGENERATED** | nothing computes it for the production model | 44 |
| **STRUCTURAL** | not a result: dataset size, design constant, definition, threshold, taxonomy | 32 |
| | **total** | **160** |

Two flags cut across the buckets:

- ⚑ **drift** — the manuscript frames the quantity as invariant, and it moved anyway. On a STRUCTURAL row that combination *is* the defect: the paper is asserting a constant that is not one.
- ⛔ **no mechanical route** — no artifact can settle it, ever. Listed in full at the end.

✓fixed marks the 12 claims the manuscript has already been corrected on since the audit snapshot — Section 4.8's close-pair paragraph and the rank appendix were rewritten to the exhaustive-enumeration definition while the audit was being written. Verified against the `.tex` and the number bank during this rendering. Their `audit` verdict below is historical; they are kept because they are the rows a checker should already pass, and because the mapping is what is being recorded, not the value.

A BANKED row may carry a `derivation`: a Python expression over its keys (`k0`, `k1`, …). The prose usually quotes a ratio, a margin, a bound or a superlative rather than a raw cell, and the derivation is what a checker must evaluate. Two derivations are **inequalities**, not values — `nd.sdpe_by_dim` and `2d.nn_wins` — because in both cases the manuscript's error is a reversed direction, which a value-only comparison would not catch.

## Claims by section

### Abstract (12)

| id | line | quantity | audit | authority / keys |
|---|---|---|---|---|
| `abs.corpus_sizes` | 90 | benchmark sizes 2,580 / 16,920 / 78 | CORRECT | `paper_tooling/tables/paper_numbers.json`<br>`2d_by_size_total_gart_2_0_n`<br>`nd_by_size_total_gart_2_0_n`<br>`tsplib_by_size_total_gart_2_0_n` |
| `abs.2d_headline` | 90 | 2D aggregate MAPE and SDPE | STALE | `2d_by_size_total_gart_2_0_mape_pct`<br>`2d_by_size_total_gart_2_0_sdpe_pct` |
| `abs.nd_headline` | 90 | ND aggregate MAPE and SDPE | STALE | `nd_by_size_total_gart_2_0_mape_pct`<br>`nd_by_size_total_gart_2_0_sdpe_pct` |
| `abs.tsplib_headline` | 90 | TSPLIB EUC_2D aggregate MAPE and SDPE | STALE | `tsplib_by_size_total_gart_2_0_mape_pct`<br>`tsplib_by_size_total_gart_2_0_sdpe_pct` |
| `abs.identical_features` ⚑ | 90 | Linear and NN share GART's feature vector | CONTRADICTED | `lgbm_model_v3/gart2_final.json`<br>`n_features` |
| `abs.nd_factor` | 90 | GART advantage over rho(d,n) on ND | STALE | `nd_by_size_total_calibrated_mst_ratio_hat_rho_d_n_mape_pct`<br>`nd_by_size_total_gart_2_0_mape_pct`<br>_derivation:_ `k0 / k1` |
| `abs.tsplib_margin` | 90 | GART margin over rho(d,n) on TSPLIB | STALE | `tsplib_by_size_total_calibrated_mst_ratio_hat_rho_d_n_mape_pct`<br>`tsplib_by_size_total_gart_2_0_mape_pct`<br>_derivation:_ `k0 - k1` |
| `abs.oracle_constant_78` | 90 | MAPE-minimising constant multiple of L_MST over the 78 EUC_2D | CORRECT | _none_ |
| `abs.tsplib_paired` | 90 | paired GART vs asymptotic-ratio difference on TSPLIB | CONTRADICTED | `paper_tooling/tables/paired_tests.csv`<br>`table=tsplib_by_size,bucket_slug=total,model_b=Asymptotic_MST -> mean_diff, ci_lo, ci_hi, wilcoxon_p` |
| `abs.dispersion_gain` | 90 | GART dispersion gain over the asymptotic ratio on TSPLIB | STALE | `tsplib_by_size_total_gart_2_0_sdpe_pct`<br>`tsplib_by_size_total_asymptotic_mst_ratio_sdpe_pct`<br>_derivation:_ `100 * (1 - k0 / k1)` |
| `abs.kwon_home` | 90 | Kwon vs GART on the Kwon domain | STALE | `classical_b_kwon_kwon_golden_wasil_sampling_region_mape_pct`<br>`classical_b_kwon_gart_2_0_mape_pct` |
| `abs.linenoise_span` | 90 | GART MAPE isotropic vs Line Noise | STALE | `2d_by_genclass_isotropic_gart_2_0_mape_pct`<br>`2d_by_genclass_linenoise_gart_2_0_mape_pct` |

<details><summary>Notes and required actions</summary>

**`abs.corpus_sizes`** — STRUCTURAL

> Dataset sizes are fixed by the corpus, not by the model. Banked only as a cross-check. HAZARD: the bank's N is per-model, so a future model that declines a EUC_2D instance would move the 78 without the corpus changing. Read the count off the coverage table, not off a model row.

**`abs.identical_features`** — STRUCTURAL ⚑

> Framed as a design invariant; it is not one. Sidecar n_features=31, Linear_V3/NN_V3 consume the 30-feature V3 vector. Five other occurrences say the same thing.

*Action:* model_registry.py has no feature-set field. Add FEATURE_SETS: {model_key -> frozenset(features)} so 'shares its feature vector' becomes a computable predicate instead of a prose assertion.

**`abs.oracle_constant_78`** — UNGENERATED

> build_paper_tables.py evaluates Fixed_Alpha=1.136 but never searches for the optimum. The value was produced by an ad-hoc grid search that persisted nothing.

*Action:* Add an oracle-constant search to build_paper_tables.py: over each instance set (78 EUC_2D, all 111, the 23 screened non-EUC_2D), grid c over true_cost/mst_length, bank c* and its MAPE as oracle_constant_<set>_{c,mape_pct}. Model-independent, so it also survives the arm A swap unchanged.

**`abs.tsplib_paired`** — GENERATED

*Action:* paired_tests.csv is written by build_paper_tables.main() but never reaches the bank. Export paired_<table>_<bucket_slug>_<model_b_slug>_{n_pairs,mean_diff,ci_lo,ci_hi,wilcoxon_p} into paper_numbers.json. This one export covers every significance claim in the paper.

</details>

### Introduction (5)

| id | line | quantity | audit | authority / keys |
|---|---|---|---|---|
| `intro.baseline_count` ⚑ | 123 | size of the comparison roster | STALE | `paper_tooling/build_paper_tables.py::TEX_MODELS + model_registry.MODEL_LABELS` |
| `intro.identical_features` ⚑ | 123 | Linear and NN share GART's feature vector | CONTRADICTED | `lgbm_model_v3/gart2_final.json`<br>`n_features` |
| `intro.superlative_all3` | 123 | GART is best on all three benchmarks among non-feature-sharing baselines | CONTRADICTED | `2d_by_size_total_*_{mape_pct,sdpe_pct}`<br>`nd_by_size_total_*_{mape_pct,sdpe_pct}`<br>`tsplib_by_size_total_*_{mape_pct,sdpe_pct}`<br>_derivation:_ `argmin over the shipped roster, excluding models that share GART's feature vector` |
| `intro.margin_range` | 123 | range of GART's margin across benchmarks | STALE | `nd_by_size_total_calibrated_mst_ratio_hat_rho_d_n_mape_pct`<br>`nd_by_size_total_gart_2_0_mape_pct`<br>`tsplib_by_size_total_calibrated_mst_ratio_hat_rho_d_n_mape_pct`<br>`tsplib_by_size_total_gart_2_0_mape_pct`<br>_derivation:_ `max(k0/k1) ... min(k2-k3) over the three benchmarks` |
| `intro.nn_beats_gart_2d` | 123 | NN_V3 beats GART on 2D | CONTRADICTED | `2d_by_size_total_gart_2_0_mape_pct`<br>`2d_by_size_total_neural_net_same_features_mape_pct`<br>_derivation:_ `k0 > k1` |

<details><summary>Notes and required actions</summary>

**`intro.baseline_count`** — STRUCTURAL ⚑

> Framed as a fixed experimental design. It moved because the roster gained a 'GART 2.0 (V3 features)' row when the model changed, and the tidy tables carry a 15th (LGBM_V4). A roster count must never be hand-typed next to a roster defined in code.

*Action:* Bank len(TEX_MODELS[roster]) - 1 per benchmark as roster_<benchmark>_n_baselines, and have splice_tables.py write the word form. Then the count cannot drift from the tables it describes.

**`intro.identical_features`** — STRUCTURAL ⚑

> Duplicate of abs.identical_features.

*Action:* See abs.identical_features.

**`intro.superlative_all3`** — BANKED

> Mechanically checkable ONLY once 'shares its feature vector' is a computable predicate; today the exclusion set is a prose judgement. Also roster-conditional: the claim's truth depends on whether LGBM_V4 ships.

*Action:* Same FEATURE_SETS addition as abs.identical_features, plus a SHIPPED_ROSTER constant so the argmin is taken over the roster that actually reaches the .tex.

**`intro.nn_beats_gart_2d`** — BANKED

> Ordering is banked. The significance attached to it in the Discussion is in paired_tests.csv (see disc.nn_paired).

</details>

### Theory (19)

| id | line | quantity | audit | authority / keys |
|---|---|---|---|---|
| `meth.alpha_sd_by_d` | 161 | training-split std(alpha) at d=2 and d=50 | UNVERIFIABLE | _none_ |
| `meth.feature_count_166` ⚑ | 166 | feature count | STALE | `lgbm_model_v3/gart2_final.json`<br>`n_features` |
| `meth.target_clip_prep` | 168 | two raw corpus alphas are clipped before the logit | CORRECT | `lgbm_model_v3/gart2_final.json`<br>`target_transform.forward` |
| `meth.feature_count_172` ⚑ | 172 | feature count | STALE | `lgbm_model_v3/gart2_final.json`<br>`n_features` |
| `meth.feature_split_19` ⚑ | 188 | feature family decomposition 11 + 19 | STALE | `lgbm_model_v3/gart2_final.json`<br>`features_in_booster_order` |
| `meth.identical_212` ⚑ | 212 | Linear and NN share GART's feature vector | CONTRADICTED | `lgbm_model_v3/gart2_final.json`<br>`n_features` |
| `meth.objective` ⚑ | 214 | training objective | CONTRADICTED | `lgbm_model_v3/gart2_final.json`<br>`target_transform.forward`<br>`target_transform.inverse` |
| `meth.inference_clip` ⚑ | 214 | existence of a post-hoc inference clip | CONTRADICTED | `lgbm_model_v3/gart2_final.json`<br>`target_transform.clip_after_inverse` |
| `meth.alphahat_range` | 214 | range of predicted alpha on the ND test split | UNVERIFIABLE | _none_ |
| `meth.optuna` ⚑ ⛔ | 214 | hyperparameter provenance | CONTRADICTED | `lgbm_model_v3/gart2_final.json`<br>`hyperparameter_provenance` |
| `meth.early_stopping` ⚑ | 214 | early-stopping metric | CONTRADICTED | `lgbm_model_v3/gart2_final.json`<br>`early_stopping_metric`<br>`early_stopping_rounds` |
| `meth.tree_count_214` | 214 | number of boosted trees | STALE | `lgbm_model_v3/gart2_final.json`<br>`num_trees`<br>`best_iteration` |
| `meth.leaves_per_tree` | 214 | realised mean leaves per tree | STALE | _none_ |
| `meth.traversals` | 219 | K = number of tree traversals per prediction | STALE | `lgbm_model_v3/gart2_final.json`<br>`num_trees` |
| `meth.depth_stats` | 219 | mean and max root-to-leaf depth | STALE | _none_ |
| `meth.comparisons` | 219 | K * Dbar comparisons per prediction | STALE | _none_ |
| `meth.complexity` ⚑ ⛔ | 219 | asymptotic cost of the feature pipeline | UNVERIFIABLE | `lgbm_model_v3/feature_engineering_gart2.py` |
| `meth.shap_count` ⚑ | 223 | feature count in the SHAP assessment | STALE | `lgbm_model_v3/gart2_final.json`<br>`n_features` |
| `meth.shap_shares` | 223 | SHAP family shares 26.4 / 22.2 / 10.0 / 4.6 | UNVERIFIABLE | _none_ |

<details><summary>Notes and required actions</summary>

**`meth.alpha_sd_by_d`** — UNGENERATED

> VERIFIED the audit's suggestion, with a correction. The audit says 'a tsp_features_v4.csv groupby' and that is the right table -- gart2_final.json records training_table=tsp_features_v4.csv. But this will not merely confirm 0.1837/0.0762: the manuscript's values came off tsp_features_v3.csv, which has 35 columns to v4's 48. Both carry 106,272 rows, so the split is probably identical and the values probably hold -- but that has to be shown, not assumed. Note also that CLAUDE.md's '90,418 rows' for the training CSV is stale.

*Action:* Groupby on the corpus: alpha = optimal_cost / mst_total_length (clip to [1,2] exactly as lgbm_model_v3/freeze_gart2_final.py does), restricted to split=='train', std by dimension. Bank as corpus_train_alpha_sd_d<d>.

**`meth.feature_count_166`** — STRUCTURAL ⚑

> One of six places the manuscript states the feature count as a design constant. It is model-dependent (30 -> 31) and will move again.

*Action:* Bank the sidecar's n_features as model_n_features and splice it, so all six occurrences come from one source.

**`meth.target_clip_prep`** — STRUCTURAL

> The clipping step is part of the frozen recipe and is recorded verbatim in the sidecar. Survives the model swap only if the next model keeps a bounded target.

**`meth.feature_count_172`** — STRUCTURAL ⚑

> Duplicate of meth.feature_count_166.

*Action:* See meth.feature_count_166.

**`meth.feature_split_19`** — STRUCTURAL ⚑

> 11 + 19 = 30 no longer adds up: greedy_nn_over_mst makes 31 and belongs to no named family. The decomposition is a taxonomy the manuscript maintains by hand.

*Action:* Add a FEATURE_FAMILIES map (feature -> family) next to the roster in model_registry.py and bank the per-family counts. Without it the taxonomy claims, the appendix tables and the SHAP family shares all stay hand-maintained.

**`meth.identical_212`** — STRUCTURAL ⚑

> Duplicate of abs.identical_features.

*Action:* See abs.identical_features.

**`meth.objective`** — STRUCTURAL ⚑

> Framed as a method definition; the recipe changed under it. The sidecar is authoritative and machine-readable.

*Action:* Bank the sidecar's target_transform strings as model_target_forward / model_target_inverse so the methods paragraph is spliced, not typed.

**`meth.inference_clip`** — STRUCTURAL ⚑

> clip_after_inverse=false. The bound is structural in the logit inverse; no clip exists to describe.

**`meth.alphahat_range`** — UNGENERATED

> VERIFIED the audit's suggestion is sufficient AND cheap. It is also the only claim in the paper that would substantiate the 'the bound is structural' argument empirically, so it is worth banking even though the clip sentence itself has to go.

*Action:* Load lgbm_model_v3/gart2_final.joblib, score the 16,920 rows of tsp_features_v4.csv with split=='test', apply the sidecar's inverse transform, bank min/max as model_alphahat_test_{min,max}. Pure inference, no retraining, seconds of work.

**`meth.optuna`** — STRUCTURAL ⚑ ⛔

> NO MECHANICAL ROUTE. The sidecar records 'V3's shipped values, frozen. Not tuned' as free text. A checker can assert the field is unchanged; it cannot verify a historical statement about how the values were obtained. Attestation, not measurement.

**`meth.early_stopping`** — STRUCTURAL ⚑

> Sidecar says cost_mape on the val split. Machine-readable, unlike the tuning provenance above.

**`meth.tree_count_214`** — GENERATED

*Action:* Mirror the sidecar's num_trees into the bank as model_num_trees. Quoted in three separate places (methods, complexity, tab:hyperparams) and wrong in all three.

**`meth.leaves_per_tree`** — UNGENERATED

> The sidecar's hyperparameters.num_leaves=148 is the CAP, not the realised mean (147.8). Quoting the cap as the realisation happens to round to the same integer here, which is exactly why nobody caught it.

*Action:* See meth.depth_stats -- same exporter.

**`meth.traversals`** — GENERATED

*Action:* Same export as meth.tree_count_214.

**`meth.depth_stats`** — UNGENERATED

> lgbm_model_v3/dump_hyperparams.py computes exactly these statistics but is hardcoded to lgbm_alpha_model_v3.joblib (the predecessor) and best_params_v3.json, and it prints to stdout without writing a file. So the code exists and the number does not.

*Action:* Repoint dump_hyperparams.py at model_registry.PRODUCTION_BOOSTER and have it write paper_tooling/tables/booster_stats.json {num_trees, leaves_mean, leaves_max, leaf_depth_mean, leaf_depth_max, per_tree_max_depth_mean, n_features}; bank those keys. One exporter settles meth.leaves_per_tree, meth.depth_stats, meth.comparisons and the tab:hyperparams row group.

**`meth.comparisons`** — UNGENERATED

*Action:* Falls out of booster_stats.json (meth.depth_stats) once that exists.

**`meth.complexity`** — STRUCTURAL ⚑ ⛔

> NO MECHANICAL ROUTE. An asymptotic statement cannot be settled by any artifact; it needs a human re-derivation against the inference module. It is on the drift list because its CONTENT moved with the model: the 31st feature adds a greedy-nearest-neighbour pass that the stated bounds do not account for. A timing curve would be corroboration, not proof.

**`meth.shap_count`** — STRUCTURAL ⚑

> Feature count again, this time inside the SHAP paragraph, where it also sets the row count of tab:shap_top.

*Action:* See meth.feature_count_166.

**`meth.shap_shares`** — UNGENERATED

> The audit's suggestion -- 're-run shap_analyzer_v3.py against gart2_final.joblib' -- is INSUFFICIENT, on three counts, all verified in the script. (1) It hardcodes DATA=tsp_features_v3.csv, which has no greedy_nn_over_mst column, so `X = X[expected]` raises KeyError on a 31-feature booster: the data path must move to tsp_features_v4.csv too. (2) It prints the top TOP_K=10 rows and writes two PNGs; it persists no numeric artifact, so a re-run leaves nothing for a checker to read. (3) It reports PER-FEATURE share_pct only. The manuscript quotes FAMILY shares ('dimension and node count 22.2%', 'centroid 10.0%'), and no grouping map exists anywhere in the repo. Repointing the model alone settles nothing.

*Action:* Rewrite as paper_tooling/shap_production.py: read PRODUCTION_BOOSTER and PRODUCTION_SIDECAR's training_table, sample the test split at seed 42, write ALL n_features rows to tables/shap_ranking.csv, and aggregate through the FEATURE_FAMILIES map (see meth.feature_split_19) into tables/shap_families.csv; bank both. Same exporter feeds tab:shap_top, which currently prints 30 rows for a 31-feature model.

</details>

### Provenance (4)

| id | line | quantity | audit | authority / keys |
|---|---|---|---|---|
| `prov.184_count` | 206 | count of instances whose stored tour disagrees with the coordinates | CORRECT | `paper_tooling/reference_tour_audit.csv` |
| `prov.drop184_gart` | 206 | GART ND MAPE with and without the 184 instances | STALE | _none_ |
| `prov.refit_clean` | 206 | refit on the cleaned training split | STALE | _none_ |
| `prov.other_estimators` | 206 | effect of dropping the 184 on every non-GART estimator | CORRECT | _none_ |

<details><summary>Notes and required actions</summary>

**`prov.184_count`** — GENERATED

*Action:* audit_reference_tours.py writes reference_tour_audit.csv and prints aggregates. Bank the counts as audit_tours_{n_corrupt,n_rounding,n_checked}. Model-independent, so it survives the arm A swap.

**`prov.drop184_gart`** — UNGENERATED

> These are E0/E1 in generalization_results.csv, which reproduce the PREDECESSOR (0.8769 / 0.8757 at 2,031 trees).

*Action:* See gen.* -- one repoint of generalization_experiments.py settles this row and all ten of Section 4.6.

**`prov.refit_clean`** — UNGENERATED

*Action:* See gen.*.

**`prov.other_estimators`** — UNGENERATED

> Model-independent, and true, but nothing regenerates it: build_paper_tables.py has no 'audited instances removed' bucket, so the delta was computed once by hand.

*Action:* Add a bucket to the 2D/ND/TSPLIB specs that masks out the instances listed in paper_tooling/corrupt_instances.txt, and bank the per-model delta. Cheap, model-independent, and it also settles concl.metric_shift_006.

</details>

### Setup (5)

| id | line | quantity | audit | authority / keys |
|---|---|---|---|---|
| `bench.daganzo_offset` | 229 | Daganzo strip-constant bias and dispersion on uniform instances | CORRECT | `classical_b_random_daganzo_sampling_region_mspe_pct`<br>`classical_b_random_daganzo_sampling_region_sdpe_pct` |
| `bench.hilbert_229` | 229 | Hilbert sort MAPE and SDPE on 2D | CORRECT | `2d_by_size_total_custom_hilbert_sort_mape_pct`<br>`2d_by_size_total_custom_hilbert_sort_sdpe_pct` |
| `bench.same30_238` ⚑ | 238 | Linear and NN share GART's feature vector | CONTRADICTED | `lgbm_model_v3/gart2_final.json`<br>`n_features` |
| `bench.same30_244` ⚑ | 244 | Linear and NN share GART's feature vector | CONTRADICTED | `lgbm_model_v3/gart2_final.json`<br>`n_features` |
| `bench.full_coverage_78` | 320 | every 2D baseline scores all 78 EUC_2D instances | CONTRADICTED | `paper_tooling/tables/coverage.csv` |

<details><summary>Notes and required actions</summary>

**`bench.same30_238`** — STRUCTURAL ⚑

> Third of six occurrences of the same false parity statement.

*Action:* See abs.identical_features.

**`bench.same30_244`** — STRUCTURAL ⚑

> Fourth occurrence, and the one the benchmark-model table leans on.

*Action:* See abs.identical_features.

**`bench.full_coverage_78`** — GENERATED

> Pre-existing, not model-driven: gated Kwon covers 5 of 78. GART covers 78/78 (it declines si1032, an EXPLICIT instance outside this stratum).

*Action:* coverage.csv is written by build_paper_tables.main() but not banked. Export coverage_<dataset>_<model_slug>_{n_used,n_declined} plus the decline reason string. Also settles app.si1032 and the '22 of 23' phrasing.

</details>

### ND results (9)

| id | line | quantity | audit | authority / keys |
|---|---|---|---|---|
| `nd.headline` | 343 | GART ND MAPE and SDPE | STALE | `nd_by_size_total_gart_2_0_mape_pct`<br>`nd_by_size_total_gart_2_0_sdpe_pct`<br>`nd_by_size_total_gart_2_0_mspe_pct` |
| `nd.rho_dn` | 343 | calibrated rho(d,n) on ND | CORRECT | `nd_by_size_total_calibrated_mst_ratio_hat_rho_d_n_mape_pct`<br>`nd_by_size_total_calibrated_mst_ratio_hat_rho_d_n_sdpe_pct` |
| `nd.factors` | 343 | GART / rho(d,n) ratio on MAPE and SDPE | STALE | `nd_by_size_total_calibrated_mst_ratio_hat_rho_d_n_mape_pct`<br>`nd_by_size_total_gart_2_0_mape_pct`<br>`nd_by_size_total_calibrated_mst_ratio_hat_rho_d_n_sdpe_pct`<br>`nd_by_size_total_gart_2_0_sdpe_pct`<br>_derivation:_ `k0/k1 and k2/k3` |
| `nd.lookup_102` | 343 | size of the calibrated rho(d,n) table | CORRECT | `calibrated_alpha_table.json`<br>`rho_dn (102 cells)`<br>`rho_d (17 cells)` |
| `nd.baseline_rows` | 343 | ND baseline MAPE/SDPE rows | CORRECT | `nd_by_size_total_linear_same_features_{mape_pct,sdpe_pct}`<br>`nd_by_size_total_calibrated_mst_ratio_hat_rho_d_{mape_pct,sdpe_pct}`<br>`nd_by_size_total_l_mathrm_mst_alpha_1_{mape_pct,sdpe_pct}`<br>`nd_by_size_total_custom_hilbert_sort_{mape_pct,sdpe_pct}`<br>`nd_by_size_total_bhh_{mape_pct,sdpe_pct}` |
| `nd.sdpe_by_size` | 345 | ND SDPE at n<=10 and n in [501,1000] | STALE | `nd_by_size_5_10_gart_2_0_sdpe_pct`<br>`nd_by_size_501_1000_gart_2_0_sdpe_pct` |
| `nd.sdpe_by_dim` | 345 | ND SDPE at d=2, d in [30,50], d=100 and its direction | CONTRADICTED | `nd_by_dim_d2_gart_2_0_sdpe_pct`<br>`nd_by_dim_d30_50_gart_2_0_sdpe_pct`<br>`nd_by_dim_d100_gart_2_0_sdpe_pct`<br>_derivation:_ `k2 > k1  (the sentence asserts a rise; the bank says 0.4507 < 0.4881, so it falls)` |
| `nd.mape_by_dim` | 345 | ND MAPE at d=2 and d=100 | STALE | `nd_by_dim_d2_gart_2_0_mape_pct`<br>`nd_by_dim_d100_gart_2_0_mape_pct`<br>_derivation:_ `k1 > k0` |
| `nd.bhh_exact_region` | 347 | BHH with and without the exact sampling region | CORRECT | `nd_by_size_total_bhh_mape_pct`<br>`nd_by_size_total_bhh_sampling_region_mape_pct` |

<details><summary>Notes and required actions</summary>

**`nd.lookup_102`** — STRUCTURAL

> A property of the frozen baseline table, not of GART. Never model-dependent.

*Action:* Bank len(rho_dn) as calib_rho_dn_cells so the count cannot drift from the JSON it describes.

**`nd.sdpe_by_dim`** — BANKED

> The single most dangerous row in the audit: a DIRECTION, not just a value, and directions are what a reader remembers. A checker that only compares numbers would flag the digits and miss that the argument inverted. Worth encoding the inequality as the check.

</details>

### 2D results (9)

| id | line | quantity | audit | authority / keys |
|---|---|---|---|---|
| `2d.sdpe_bound` | 351 | tightest SDPE bound across 2D size buckets | STALE | `2d_by_size_5_10_gart_2_0_sdpe_pct`<br>`2d_by_size_11_50_gart_2_0_sdpe_pct`<br>`2d_by_size_51_100_gart_2_0_sdpe_pct`<br>`2d_by_size_101_500_gart_2_0_sdpe_pct`<br>`2d_by_size_501_1000_gart_2_0_sdpe_pct`<br>_derivation:_ `ceil(max(k0..k4) * 10) / 10` |
| `2d.sdpe_bucket_501` | 351 | 2D SDPE in the [501,1000] bucket | STALE | `2d_by_size_501_1000_gart_2_0_sdpe_pct` |
| `2d.half_of_1157` | 351 | GART aggregate SDPE vs the alpha=1 floor on 2D | STALE | `2d_by_size_total_gart_2_0_sdpe_pct`<br>`2d_by_size_total_l_mathrm_mst_alpha_1_sdpe_pct`<br>_derivation:_ `k0 < k1 / 2` |
| `2d.headline` | 362 | GART 2D MAPE and SDPE | STALE | `2d_by_size_total_gart_2_0_mape_pct`<br>`2d_by_size_total_gart_2_0_sdpe_pct` |
| `2d.baseline_rows` | 362 | 2D baseline MAPE/SDPE rows | CORRECT | `2d_by_size_total_calibrated_mst_ratio_hat_rho_d_n_{mape_pct,sdpe_pct}`<br>`2d_by_size_total_gart_1_0_{mape_pct,sdpe_pct}`<br>`2d_by_size_total_asymptotic_mst_ratio_{mape_pct,sdpe_pct}`<br>`2d_by_size_total_l_mathrm_mst_alpha_1_{mape_pct,sdpe_pct}` |
| `2d.nn_wins` | 362 | NN_V3 vs GART on 2D | CONTRADICTED | `2d_by_size_total_neural_net_same_features_mape_pct`<br>`2d_by_size_total_neural_net_same_features_sdpe_pct`<br>`2d_by_size_total_gart_2_0_mape_pct`<br>`2d_by_size_total_gart_2_0_sdpe_pct`<br>_derivation:_ `k0 < k2 (MAPE) and k1 < k3 (SDPE) -- only the second holds` |
| `2d.hilbert_362` | 362 | Hilbert sort on 2D | CORRECT | `2d_by_size_total_custom_hilbert_sort_mape_pct`<br>`2d_by_size_total_custom_hilbert_sort_sdpe_pct` |
| `2d.genclass_span` | 364 | GART MAPE isotropic vs Line Noise | STALE | `2d_by_genclass_isotropic_gart_2_0_mape_pct`<br>`2d_by_genclass_linenoise_gart_2_0_mape_pct` |
| `2d.genclass_baselines` | 364 | baseline MAPE across generator classes | CORRECT | `2d_by_genclass_{isotropic,linenoise}_calibrated_mst_ratio_hat_rho_d_n_mape_pct`<br>`2d_by_genclass_{isotropic,linenoise}_asymptotic_mst_ratio_mape_pct`<br>`2d_by_genclass_{isotropic,linenoise}_l_mathrm_mst_alpha_1_mape_pct` |

<details><summary>Notes and required actions</summary>

**`2d.sdpe_bound`** — BANKED

> A bound, not a cell. It stayed stale because 6.3 was still TRUE of the new model -- just no longer tight. A value-only checker cannot see a loose bound; the derivation has to be the tight one.

</details>

### TSPLIB (12)

| id | line | quantity | audit | authority / keys |
|---|---|---|---|---|
| `tsplib.bucket_design` | 368 | 78 instances split 23 / 16 / 39 | CORRECT | `paper_tooling/build_paper_tables.py::B_TSPLIB`<br>`tsplib_by_size_{51_150,151_400,gt400}_gart_2_0_n` |
| `tsplib.headline` | 380 | GART TSPLIB MAPE and SDPE | STALE | `tsplib_by_size_total_gart_2_0_sdpe_pct`<br>`tsplib_by_size_total_gart_2_0_mape_pct` |
| `tsplib.baseline_rows` | 380 | TSPLIB baseline SDPE/MAPE rows | CORRECT | `tsplib_by_size_total_calibrated_mst_ratio_hat_rho_d_n_{sdpe_pct,mape_pct}`<br>`tsplib_by_size_total_asymptotic_mst_ratio_{sdpe_pct,mape_pct}`<br>`tsplib_by_size_total_gart_1_0_{sdpe_pct,mape_pct}` |
| `tsplib.alpha_moments` | 380 | mean and sd of true alpha over the 78 EUC_2D instances | CORRECT | _none_ |
| `tsplib.oracle_constant` | 380 | c* = 1.1275 and its 3.54% MAPE over the 78 | CORRECT | _none_ |
| `tsplib.bound_margin` | 380 | GART's margin over the best constant | STALE | _none_ |
| `tsplib.dispersion_gap` | 380 | GART SDPE vs asymptotic-ratio SDPE on TSPLIB | STALE | `tsplib_by_size_total_gart_2_0_sdpe_pct`<br>`tsplib_by_size_total_asymptotic_mst_ratio_sdpe_pct` |
| `tsplib.gt400_overlap` | 382 | overlap of the two bootstrap SDPE intervals in the n>400 bucket | CORRECT | `tsplib_by_size_gt400_gart_2_0_sdpe_lo`<br>`tsplib_by_size_gt400_gart_2_0_sdpe_hi`<br>`tsplib_by_size_gt400_asymptotic_mst_ratio_sdpe_lo`<br>`tsplib_by_size_gt400_asymptotic_mst_ratio_sdpe_hi`<br>_derivation:_ `k1 > k2 and k3 > k0` |
| `tsplib.n_gt_10000` | 382 | GART vs asymptotic ratio on the 5 instances with n>10,000 | CONTRADICTED | _none_ |
| `tsplib.d18512_ratio` | 382 | largest TSPLIB instance relative to the n<=1000 training cap | CORRECT | — |
| `tsplib.rho_d_vs_dn` | 384 | rho(2) vs rho(2,n) on TSPLIB | CORRECT | `tsplib_by_size_total_calibrated_mst_ratio_hat_rho_d_mape_pct`<br>`tsplib_by_size_total_calibrated_mst_ratio_hat_rho_d_n_mape_pct` |
| `tsplib.classical_rows` | 384 | classical estimators on TSPLIB EUC_2D | CORRECT | `classical_a_tsplib_cavdar_sokol_mape_pct`<br>`classical_a_tsplib_bhh_mape_pct`<br>`classical_a_tsplib_chien_extrapolated_mape_pct`<br>`classical_a_tsplib_daganzo_mape_pct`<br>`classical_a_tsplib_kwon_golden_wasil_extrapolated_mape_pct` |

<details><summary>Notes and required actions</summary>

**`tsplib.bucket_design`** — STRUCTURAL

> Bucket edges are a prespecified design choice; the occupancy counts follow from the corpus. Model-independent unless a model declines a EUC_2D instance.

**`tsplib.alpha_moments`** — UNGENERATED

> Model-independent corpus statistic, and it will NOT move when the model swaps -- but nothing exports it, so it is unprotected against a change in the screen that selects the 78.

*Action:* Bank alpha moments per instance set from the ground-truth frame that build_paper_tables already loads: corpus_alpha_<set>_{n,mean,sd,min,max}. Same exporter also settles app.alpha_range_22.

**`tsplib.oracle_constant`** — UNGENERATED

*Action:* See abs.oracle_constant_78 -- one oracle-constant exporter covers the 78, the 111 and the 23.

**`tsplib.bound_margin`** — UNGENERATED

> One banked term, one ungenerated term. Blocked on the oracle-constant exporter.

*Action:* See abs.oracle_constant_78.

**`tsplib.n_gt_10000`** — UNGENERATED

> No such bucket exists. B_TSPLIB stops at 'n>400', so the slice was cut by hand and never regenerated. It is the slice the paper leans on to concede a near-tie at large n -- and the concession is now false.

*Action:* Add ('$n>10{,}000$', 'gt10000', _rng('n', 10001, 10**9)) to B_TSPLIB, or a separate post-hoc spec so it is not confused with the prespecified buckets. Then it is BANKED like every other bucket.

**`tsplib.d18512_ratio`** — STRUCTURAL

> 18,512 / 1,000. Both terms are corpus/design constants. Would only move if the training-size cap moved.

**`tsplib.rho_d_vs_dn`** — BANKED

> The constant 1.2610 itself is STRUCTURAL: calibrated_alpha_table.json rho_d['2'].

</details>

### Matched domain (7)

| id | line | quantity | audit | authority / keys |
|---|---|---|---|---|
| `matched.classical_rows` | 390 | classical estimators, full vs matched domain | CORRECT | `classical_a_2d_{bhh,cavdar_sokol,daganzo}_mape_pct`<br>`classical_b_random_{bhh,cavdar_sokol,daganzo,chien,kwon}_sampling_region_{mape_pct,mspe_pct}`<br>`classical_b_kwon_kwon_golden_wasil_sampling_region_mape_pct` |
| `matched.kwon_domain` | 392 | GART vs Kwon on the Kwon domain | STALE | `classical_b_kwon_gart_2_0_mape_pct`<br>`classical_b_kwon_kwon_golden_wasil_sampling_region_mape_pct` |
| `matched.chien_domain` | 392 | GART vs Chien on the Chien domain | STALE | `classical_b_chien_gart_2_0_mape_pct`<br>`classical_b_chien_chien_sampling_region_mape_pct` |
| `matched.uniform_210` | 392 | GART vs BHH and the alpha=1 floor on the uniform subset | STALE | `classical_b_random_gart_2_0_mape_pct`<br>`classical_b_random_bhh_sampling_region_mape_pct`<br>`classical_b_random_l_mathrm_mst_alpha_1_mape_pct` |
| `matched.nn_edges` | 392 | NN_V3 vs GART on the 210 uniform instances | CONTRADICTED | `classical_b_random_gart_2_0_mape_pct`<br>`classical_b_random_neural_net_same_features_mape_pct`<br>_derivation:_ `k1 < k0` |
| `matched.nn_pvalue` | 392 | significance of the NN-GART difference on the 210 | STALE | `paper_tooling/tables/paired_tests.csv`<br>`table=classical,bucket_slug=b_random,model_b=NN_V3 -> wilcoxon_p` |
| `matched.kwon_factor` | 392 | GART / Kwon ratio on the Kwon domain | STALE | `classical_b_kwon_kwon_golden_wasil_sampling_region_mape_pct`<br>`classical_b_kwon_gart_2_0_mape_pct`<br>_derivation:_ `k0 / k1` |

<details><summary>Notes and required actions</summary>

**`matched.nn_pvalue`** — GENERATED

> p=0.73 is now the KWON-DOMAIN comparison (n=80); the 210-instance comparison is p=0.069. A p-value that migrated between rows is exactly what a bucket_slug-keyed export prevents.

*Action:* See abs.tsplib_paired.

</details>

### Generalization (9)

| id | line | quantity | audit | authority / keys |
|---|---|---|---|---|
| `gen.held_out_sampling` | 396 | GART ND MAPE as the interpolation reference | STALE | _none_ |
| `gen.e0_bitforbit` | 396 | E0 baseline refit reproduces the shipped model | CONTRADICTED | _none_ |
| `gen.16846` | 396 | cleaned-split test row count and its MAPE | STALE | _none_ |
| `gen.leave_dims_out` | 398 | leave-dimensions-out refit at d=15 and d=25 | STALE | _none_ |
| `gen.neighbour_dims` | 398 | unaffected neighbouring dimensions | STALE | _none_ |
| `gen.signed_error_d15` | 398 | mean signed error shift at d=15 | STALE | _none_ |
| `gen.leave_large_n_out` | 398 | leave-large-n-out refit | STALE | _none_ |
| `gen.within_range` | 400 | held-out regimes land inside the released model's range | CONTRADICTED | _none_ |
| `gen.cost_028` | 400 | cost of withholding a dimension | STALE | _none_ |

<details><summary>Notes and required actions</summary>

**`gen.held_out_sampling`** — UNGENERATED

*Action:* GEN-REPOINT (see note on gen.e0_bitforbit).

**`gen.e0_bitforbit`** — UNGENERATED

> CONFIRMED at code level. generalization_experiments.py pins DATA_FILE=tsp_features_v3.csv, PARAMS_FILE=lgbm_model_v3/best_params_v3.json and SHIPPED_MODEL_FILE=lgbm_model_v3/lgbm_alpha_model_v3.joblib -- all three are the PREDECESSOR. Its E0 row reproduces LGBM_V3 bit-for-bit (0.8769 MAPE, 2,031 trees) and the manuscript reports that as the production model's reproducibility calibration. Every number in Section 4.6 inherits the error.

*Action:* GEN-REPOINT: point generalization_experiments.py at model_registry.PRODUCTION_BOOSTER / PRODUCTION_SIDECAR and read the recipe (training_table, target transform, monotone constraints, early-stopping metric, frozen hyperparameters) from the sidecar rather than restating it. BLOCKED IN THIS TASK: four refits is training, and this task forbids it. Also worth gating -- have E0 assert its output hashes to PRODUCTION_SIDECAR's sha256 and fail loudly otherwise, which is the check that would have caught this.

**`gen.16846`** — UNGENERATED

> Row count 16,846 is correct and structural; the MAPE attached to it is the predecessor's.

*Action:* GEN-REPOINT.

**`gen.leave_dims_out`** — UNGENERATED

*Action:* GEN-REPOINT.

**`gen.neighbour_dims`** — UNGENERATED

*Action:* GEN-REPOINT.

**`gen.signed_error_d15`** — UNGENERATED

*Action:* GEN-REPOINT.

**`gen.leave_large_n_out`** — UNGENERATED

*Action:* GEN-REPOINT.

**`gen.within_range`** — UNGENERATED

> The argument INVERTS, not just the digits: 0.85-0.90% was inside the predecessor's range and is well outside the production model's 0.62%. A number-only checker would report a small delta on a sentence whose conclusion has reversed.

*Action:* GEN-REPOINT.

**`gen.cost_028`** — UNGENERATED

> Also internally inconsistent with the 0.35 pp at line 486.

*Action:* GEN-REPOINT.

</details>

### Degenerate geometry (12)

| id | line | quantity | audit | authority / keys |
|---|---|---|---|---|
| `aug.linenoise_vs_isotropic` | 404 | GART MAPE on Line Noise vs isotropic | STALE | `2d_by_genclass_linenoise_gart_2_0_mape_pct`<br>`2d_by_genclass_isotropic_gart_2_0_mape_pct` |
| `aug.linenoise_mspe` | 404 | Line Noise MSPE | STALE | `2d_by_genclass_linenoise_gart_2_0_mspe_pct` |
| `aug.slope_corr` | 404 | regression of predicted alpha on true alpha, Line Noise n>=200 | UNVERIFIABLE | _none_ |
| `aug.alpha_gt_145` | 404 | alpha>1.45 confined to n<=10 in the training split | CORRECT | _none_ |
| `aug.success_criteria` ⚑ ⛔ | 406 | prespecified adoption thresholds for the augmentation | STALE | — |
| `aug.round1` | 408 | first augmentation round | UNVERIFIABLE | _none_ |
| `aug.round2` | 408 | second augmentation round | UNVERIFIABLE | _none_ |
| `aug.corpus_rho_alpha` | 408 | median rho and alpha of the augmentation corpus | CORRECT | _none_ |
| `aug.mean_pred_alpha` | 410 | mean predicted alpha before/after augmentation and the [401,1000] bucket | UNVERIFIABLE | _none_ |
| `aug.half_linenoise` | 410 | recall experiment slope and MAPE | UNVERIFIABLE | _none_ |
| `aug.geometry_facts` | 412 | kurtosis, width ratio, crossover at rho~8, alpha offset | CORRECT | _none_ |
| `aug.grid_mspe` | 412 | grid sub-generator MSPE before/after augmentation | UNVERIFIABLE | _none_ |

<details><summary>Notes and required actions</summary>

**`aug.linenoise_vs_isotropic`** — BANKED

> Internally inconsistent in the manuscript: 11.59 here, 11.60 at lines 364 and 428, for the same quantity. Banking it removes the possibility.

**`aug.slope_corr`** — UNGENERATED

> The audit proposes refitting on gart2_final predictions for the 210 Line Noise rows. That WOULD settle it, with one correction: the sentence restricts to n>=200, so the fit is not over all 210. paper_tooling/armA_verify_oracle_constant.csv carries slope_frozen for line_noise at n=210 -- a different subset, and part of the arm study, so it is not the authority here.

*Action:* Bank an alpha-on-alpha OLS per stratum from the per-instance predictions build_paper_tables already has in hand: alphafit_<bucket>_{slope,intercept,pearson_r,n}, with the n>=200 restriction as an explicit bucket. Settles this, aug.half_linenoise and concl.slope_corr.

**`aug.alpha_gt_145`** — UNGENERATED

> Model-independent corpus fact, never re-derived.

*Action:* Same corpus-statistics exporter as meth.alpha_sd_by_d: bank corpus_train_alpha_gt145_max_n or the n-histogram of alpha>1.45.

**`aug.success_criteria`** — STRUCTURAL ⚑ ⛔

> NO MECHANICAL ROUTE to the prespecification itself -- that a threshold was fixed in advance is a fact about the process, not a value. FLAGGED because the thresholds are stated as absolute budgets ('<=0.05 pp ND regression') measured against a baseline that no longer ships, so a constant reads as having moved. Whether the criteria are MET is checkable once aug.slope_corr is banked.

**`aug.round1`** — UNGENERATED

> augmentation_experiment.py pins tsp_features_v3.csv, best_params_v3.json and lgbm_alpha_model_v3.joblib; augmentation_results.csv predates the model change.

*Action:* AUG-REPOINT: same treatment as GEN-REPOINT, and equally blocked -- both rounds retrain. If the augmentation study is not re-run, the honest move is to label Section 4.7's round-1/round-2 numbers explicitly as the predecessor's, which is a prose decision, not a tooling one.

**`aug.round2`** — UNGENERATED

> augmentation_v2_experiment.py, same three hardcodes.

*Action:* AUG-REPOINT.

**`aug.corpus_rho_alpha`** — UNGENERATED

> Model-independent corpus geometry; nothing exports it.

*Action:* Bank descriptive statistics of the augmentation corpora alongside the corpus-statistics exporter.

**`aug.mean_pred_alpha`** — UNGENERATED

*Action:* AUG-REPOINT.

**`aug.half_linenoise`** — UNGENERATED

*Action:* AUG-REPOINT (plus the alpha-on-alpha exporter of aug.slope_corr).

**`aug.geometry_facts`** — UNGENERATED

> Model-independent geometry, never re-derived.

*Action:* Corpus-statistics exporter.

**`aug.grid_mspe`** — UNGENERATED

*Action:* AUG-REPOINT.

</details>

### Discussion (21)

| id | line | quantity | audit | authority / keys |
|---|---|---|---|---|
| `disc.rank_gart` | 418 | GART Spearman/Kendall on all three benchmarks | STALE | `rank_2d_gart_2_0_{spearman_rho,kendall_tau}`<br>`rank_nd_gart_2_0_{spearman_rho,kendall_tau}`<br>`rank_tsplib_euc2d_gart_2_0_{spearman_rho,kendall_tau}` |
| `disc.rank_control` | 418 | alpha=1 control rank statistics | CORRECT | `rank_2d_l_mathrm_mst_alpha_1_{spearman_rho,kendall_tau}`<br>`rank_nd_l_mathrm_mst_alpha_1_{spearman_rho,kendall_tau}`<br>`rank_tsplib_euc2d_l_mathrm_mst_alpha_1_{spearman_rho,kendall_tau}` |
| `disc.pairs_2d` ⚑ ✓fixed | 420 | 2D close-pair universe at the 5% threshold | STALE | `paper_tooling/tables/paper_numbers.json`<br>`rank_2d_gart_2_0_close5_pairs` |
| `disc.close5_2d` ✓fixed | 420 | GART 2D close-pair ordering accuracy at 5% | STALE | `rank_2d_gart_2_0_close5_pct` |
| `disc.pairs_nd` ⚑ ✓fixed | 420 | ND close-pair sampling design | UNVERIFIABLE | `paper_tooling/tables/paper_numbers.json`<br>`rank_nd_gart_2_0_pair_mode`<br>`rank_nd_gart_2_0_close5_pairs`<br>`rank_nd_gart_2_0_pair_seed` |
| `disc.close5_nd` ✓fixed | 420 | GART ND close-pair ordering accuracy at 5% | UNVERIFIABLE | `rank_nd_gart_2_0_close5_pct` |
| `disc.close5_tsplib` ✓fixed | 420 | GART TSPLIB close-pair ordering accuracy at 5% | STALE | `rank_tsplib_euc2d_gart_2_0_close5_pct`<br>`rank_tsplib_euc2d_gart_2_0_close5_pairs` |
| `disc.close5_control` ✓fixed | 420 | alpha=1 control close-pair accuracy on all three | CORRECT | `rank_2d_l_mathrm_mst_alpha_1_close5_pct`<br>`rank_nd_l_mathrm_mst_alpha_1_close5_pct`<br>`rank_tsplib_euc2d_l_mathrm_mst_alpha_1_close5_pct` |
| `disc.gain_nd` ✓fixed | 420 | ND close-pair gain over the control | UNVERIFIABLE | `rank_nd_gart_2_0_close5_pct`<br>`rank_nd_l_mathrm_mst_alpha_1_close5_pct`<br>_derivation:_ `k0 - k1` |
| `disc.gain_2d` ✓fixed | 420 | 2D close-pair gain over the control | CONTRADICTED | `rank_2d_gart_2_0_close5_pct`<br>`rank_2d_l_mathrm_mst_alpha_1_close5_pct`<br>_derivation:_ `k0 - k1` |
| `disc.gain_tsplib` ✓fixed | 420 | TSPLIB close-pair gain over the control | STALE | `rank_tsplib_euc2d_gart_2_0_close5_pct`<br>`rank_tsplib_euc2d_l_mathrm_mst_alpha_1_close5_pct`<br>_derivation:_ `k0 - k1` |
| `disc.close10` ✓fixed | 420 | GART close-pair accuracy at the 10% threshold | STALE | `rank_2d_gart_2_0_close10_pct`<br>`rank_nd_gart_2_0_close10_pct`<br>`rank_tsplib_euc2d_gart_2_0_close10_pct` |
| `disc.nn_close_pairs` ✓fixed | 420 | NN_V3 vs GART on TSPLIB close pairs | STALE | `rank_tsplib_euc2d_neural_net_same_features_close5_pct`<br>`rank_tsplib_euc2d_gart_2_0_close5_pct`<br>_derivation:_ `k0 > k1` |
| `disc.r2_alpha_gt400` | 424 | R^2_alpha in the TSPLIB n>400 bucket | STALE | `tsplib_by_size_gt400_gart_2_0_r2_alpha` |
| `disc.mape_gt400` | 424 | GART MAPE in the TSPLIB n>400 bucket | STALE | `tsplib_by_size_gt400_gart_2_0_mape_pct` |
| `disc.asym_paired` | 426 | paired GART vs asymptotic ratio on TSPLIB | CONTRADICTED | `paper_tooling/tables/paired_tests.csv`<br>`table=tsplib_by_size,bucket_slug=total,model_b=Asymptotic_MST` |
| `disc.dispersion_gain` | 426 | GART SDPE vs asymptotic-ratio SDPE on TSPLIB | STALE | `tsplib_by_size_total_gart_2_0_sdpe_pct`<br>`tsplib_by_size_total_asymptotic_mst_ratio_sdpe_pct` |
| `disc.nn_paired` | 426 | paired NN vs GART difference on 2D | CONTRADICTED | `paper_tooling/tables/paired_tests.csv`<br>`table=2d_by_size,bucket_slug=total,model_b=NN_V3 -> mean_diff, ci_lo, ci_hi, wilcoxon_p` |
| `disc.boosting_conclusion` ⛔ | 426 | attribution of the 2D advantage to features rather than boosting | CONTRADICTED | `paper_tooling/tables/paired_tests.csv` |
| `disc.genclass_five` | 428 | GART MAPE across the five generator classes | STALE | `2d_by_genclass_isotropic_gart_2_0_mape_pct`<br>`2d_by_genclass_biased_gart_2_0_mape_pct`<br>`2d_by_genclass_clustered_gart_2_0_mape_pct`<br>`2d_by_genclass_geometric_gart_2_0_mape_pct`<br>`2d_by_genclass_linenoise_gart_2_0_mape_pct` |
| `disc.six_times` | 428 | Line Noise / isotropic error ratio | STALE | `2d_by_genclass_linenoise_gart_2_0_mape_pct`<br>`2d_by_genclass_isotropic_gart_2_0_mape_pct`<br>_derivation:_ `k0 / k1` |

<details><summary>Notes and required actions</summary>

**`disc.rank_gart`** — BANKED

> The audit called tab:rank unbacked because gen_paper_numbers.py persisted nothing. That is no longer true: build_paper_tables.py now computes rank statistics and banks them. Re-checked against the live bank.

**`disc.pairs_2d`** — STRUCTURAL ⚑

> FLAGGED, and this one did NOT move with the model -- it moved with the GENERATOR. The manuscript says 74,835; the bank says 74,759, because build_paper_tables.py redefined the qualifying predicate (exact ties now excluded, and the enumeration is exhaustive rather than sampled). A pair universe is a design constant; when one silently changes underneath a fixed number, every percentage keyed to it moves too.

**`disc.pairs_nd`** — STRUCTURAL ⚑

> FLAGGED. The audit says the fixed-seed sampler 'lives only in gen_paper_numbers.py and persists nothing'. That is now out of date and the suggested fix -- re-run gen_paper_numbers.py -- would REINTRODUCE the superseded design. build_paper_tables.py enumerates ND pairs EXHAUSTIVELY (1,662,781 qualifying at 5%, 3,290,075 at 10%), records pair_mode='exhaustive' and sets pair_seed/pair_draws to 'n/a' because no draw is made. The 46,434-pair sample no longer exists as a concept. Line 830's appendix sentence describes the same dead design.

**`disc.close5_nd`** — BANKED

> Reclassified from the audit's UNVERIFIABLE: it is banked now, under the exhaustive definition (92.37%), not the sampled one.

**`disc.gain_nd`** — BANKED

> Reclassified from UNVERIFIABLE: both terms are banked.

**`disc.asym_paired`** — GENERATED

> Same statistic as abs.tsplib_paired, quoted twice with the same stale value. Direction of the conclusion reverses: p moved from 0.77 to 4.85e-3.

*Action:* See abs.tsplib_paired.

**`disc.nn_paired`** — GENERATED

> The SIGN flips. The CI [0.27,0.43] excluded zero; the current one [-0.164,-0.008] sits on the other side.

*Action:* See abs.tsplib_paired.

**`disc.boosting_conclusion`** — GENERATED ⛔

> NO MECHANICAL ROUTE to the claim itself: it is a causal attribution, and no artifact can certify 'the feature set is the source'. Its PREMISE (that the NN beats the boosted ensemble) is banked, and that premise is now false, so the sentence falls with it. A checker can flag the premise and must not be asked to judge the inference.

*Action:* Bind this sentence to disc.nn_paired in the manifest so that when the premise flips, the conclusion is surfaced for human review.

</details>

### Timing (4)

| id | line | quantity | audit | authority / keys |
|---|---|---|---|---|
| `time.decomposition` | 430 | 82.6 / 16.3 / 1.2 time decomposition | PENDING | _none_ |
| `time.median_vs_reference` | 430 | GART median prediction time vs reference tour time on ND | PENDING | _none_ |
| `time.bucket_501` | 430 | reference tour vs GART at n in [501,1000] | PENDING | _none_ |
| `time.d18512` | 430 | GART prediction time on d18512 | PENDING | _none_ |

<details><summary>Notes and required actions</summary>

**`time.decomposition`** — UNGENERATED

> CONFIRMED as genuinely sourceless, and worse than the audit implies. tables/table_time_breakdown.csv is dated Apr 18, has no MST column at all (only pct_features / pct_inference / pct_other), reports 74.75 / 25.03 / 0.22 for TSPLIB, and carries the stale ND count 16,907. It does not back the sentence even for the predecessor. Meanwhile all_models_tsplib.csv tags all 109 GART_2.0 rows 'pending_no_serial_measurement', and build_paper_tables' publishable_times() guard withholds the whole group, which is why tsplib_by_size_total_gart_2_0_time_ms is null in the bank.

*Action:* Requires a low-contention serial run, then restore_tsplib_serial_timings.py, then a decomposition exporter that separates MST construction from the rest of feature extraction. Route exists; it is a measurement, not a computation.

**`time.median_vs_reference`** — UNGENERATED

> Numbers EXIST but are not trustworthy, which is the worst state to be in. nd_by_size_total_gart_2_0_time_ms = 121.5 ms is banked against the manuscript's 171 ms, so the sentence's direction reverses. But the ND frame carries no timing_provenance column, so publishable_times() passes it through unguarded -- the 121.5 ms has the same contended origin the TSPLIB guard exists to suppress. Do not adopt it.

*Action:* Extend timing_provenance to the 2D and ND benchmark CSVs so the guard covers them, then re-measure. Until then the bank's 2D/ND time_ms keys should be treated as UNPUBLISHABLE, not as authority. The reference-tour times are computed by _ref_times() and reach the .tex fragments but are never banked -- export them as ref_tour_<table>_<bucket_slug>_time_ms.

**`time.bucket_501`** — UNGENERATED

> Bank says 465.0 ms against the manuscript's 272 ms; same provenance problem as time.median_vs_reference.

*Action:* See time.median_vs_reference.

**`time.d18512`** — UNGENERATED

> all_models_tsplib.csv records total_time_s=0.7525 for GART_2.0 on d18512, tagged pending. Neither 127 ms nor 752 ms is a clean measurement.

*Action:* See time.decomposition.

</details>

### Application (8)

| id | line | quantity | audit | authority / keys |
|---|---|---|---|---|
| `app.oracle_111` | 434 | c* = 1.1341 over the full 111-instance TSPLIB set | CORRECT | _none_ |
| `app.oracle_23` | 434 | c* = 1.1718 and 7.55% over the 23 screened non-EUC_2D | CORRECT | _none_ |
| `app.two_of_thirty` ⚑ | 436 | MDS pipeline omits 2 of the model's features | STALE | `lgbm_model_v3/gart2_final.json`<br>`n_features` |
| `app.tsplib_taxonomy` | 438 | 33 non-EUC_2D split 4/2/10/17, leaving 23 including seven EXPLICIT | CORRECT | `tsplib_benchmark/exclusions.py`<br>`tsplib_nonEuc_{att,ceil2d,geo,explicit}_*_n` |
| `app.mds_cap` | 442 | cases the embedding-dimension cap keeps below 99.9% stress | CORRECT | `paper_reference/mds_distortion_screened.csv` |
| `app.si1032` | 448 | si1032 declined on the greedy-ratio gate; 22 scored of 23 | CORRECT | `paper_tooling/tables/coverage.csv` |
| `app.noneuc_results` | 480 | non-Euclidean MDS pipeline results | CORRECT | `tsplib_nonEuc_total_gart_2_0_{sdpe_pct,mape_pct}`<br>`tsplib_nonEuc_total_fixed_alpha_1_136_{sdpe_pct,mape_pct}`<br>`tsplib_nonEuc_total_gart_2_0_v3_features_{sdpe_pct,mape_pct}`<br>`tsplib_nonEuc_att_gart_2_0_mape_pct`<br>`tsplib_nonEuc_ceil2d_*_sdpe_pct` |
| `app.alpha_range_22` | 480 | range of true alpha over the 22 accepted non-EUC_2D instances | CORRECT | _none_ |

<details><summary>Notes and required actions</summary>

**`app.oracle_111`** — UNGENERATED

*Action:* See abs.oracle_constant_78.

**`app.oracle_23`** — UNGENERATED

*Action:* See abs.oracle_constant_78.

**`app.two_of_thirty`** — STRUCTURAL ⚑

> The denominator is the feature count, so it moved with it; the numerator (two omitted features) is a property of the MDS pipeline and did not.

*Action:* See meth.feature_count_166.

**`app.tsplib_taxonomy`** — STRUCTURAL

> A corpus taxonomy fixed by the TSPLIB headers and the screen. Never model-dependent -- but see app.seven_explicit, where the manuscript silently reuses the taxonomy count as a scoring count.

**`app.mds_cap`** — GENERATED

*Action:* audit_mds_distortion.py writes mds_distortion_screened.csv. Bank the below-target counts as mds_below_target_{att,geo,explicit,total} and mds_cases_total.

**`app.si1032`** — GENERATED

> Already corrected in the manuscript. The gate floor 1.035 is STRUCTURAL (an estimator config constant); the declined instance and its ratio 1.0260 come from coverage.csv.

*Action:* See bench.full_coverage_78 for the coverage export.

**`app.alpha_range_22`** — UNGENERATED

> Model-independent, but tied to a model-dependent instance set (the 22 GART accepts), which is precisely the kind of coupling that breaks silently on a model swap.

*Action:* Same corpus-alpha exporter as tsplib.alpha_moments, keyed on the accepted set rather than the screened set, so the coupling is explicit.

</details>

### Conclusion (15)

| id | line | quantity | audit | authority / keys |
|---|---|---|---|---|
| `concl.feature_families` ⚑ | 484 | feature count and family taxonomy | STALE | `lgbm_model_v3/gart2_final.json`<br>`n_features`<br>`features_in_booster_order` |
| `concl.sharing_vector` ⚑ | 484 | Linear and NN share GART's feature vector | CONTRADICTED | `lgbm_model_v3/gart2_final.json`<br>`n_features` |
| `concl.lowest_sdpe` | 484 | GART has the lowest SDPE on ND and TSPLIB | CORRECT | `nd_by_size_total_*_sdpe_pct`<br>`tsplib_by_size_total_*_sdpe_pct`<br>_derivation:_ `argmin over the shipped roster` |
| `concl.lowest_2d` | 484 | GART has the lowest 2D SDPE among non-feature-sharing estimators | CONTRADICTED | `2d_by_size_total_gart_2_0_sdpe_pct`<br>`2d_by_size_total_neural_net_same_features_sdpe_pct`<br>_derivation:_ `k0 < k1` |
| `concl.nd_factor` | 484 | GART / rho(d,n) on ND | STALE | `nd_by_size_total_calibrated_mst_ratio_hat_rho_d_n_mape_pct`<br>`nd_by_size_total_gart_2_0_mape_pct`<br>_derivation:_ `k0 / k1` |
| `concl.kwon_factor` | 484 | GART / Kwon on the Kwon domain | STALE | `classical_b_kwon_kwon_golden_wasil_sampling_region_mape_pct`<br>`classical_b_kwon_gart_2_0_mape_pct`<br>_derivation:_ `k0 / k1` |
| `concl.tsplib_margin` | 484 | GART margin over the asymptotic ratio on TSPLIB | STALE | `tsplib_by_size_total_asymptotic_mst_ratio_mape_pct`<br>`tsplib_by_size_total_gart_2_0_mape_pct`<br>_derivation:_ `k0 - k1` |
| `concl.paired_zero` | 484 | significance of that margin | CONTRADICTED | `paper_tooling/tables/paired_tests.csv` |
| `concl.dispersion_gain` | 484 | dispersion gain on TSPLIB | STALE | `tsplib_by_size_total_gart_2_0_sdpe_pct`<br>`tsplib_by_size_total_asymptotic_mst_ratio_sdpe_pct`<br>_derivation:_ `100 * (1 - k0 / k1)` |
| `concl.nn_wins` | 484 | NN beats the boosted ensemble on 2D | CONTRADICTED | `2d_by_size_total_neural_net_same_features_mape_pct`<br>`2d_by_size_total_gart_2_0_mape_pct`<br>_derivation:_ `k0 < k1` |
| `concl.noneuc` | 484 | non-Euclidean results restated | CORRECT | `tsplib_nonEuc_total_gart_2_0_mape_pct`<br>`tsplib_nonEuc_total_fixed_alpha_1_136_mape_pct` |
| `concl.linenoise` | 486 | Line Noise vs isotropic error | STALE | `2d_by_genclass_linenoise_gart_2_0_mape_pct`<br>`2d_by_genclass_isotropic_gart_2_0_mape_pct` |
| `concl.slope_corr` | 486 | augmentation slope and correlation movement | UNVERIFIABLE | _none_ |
| `concl.withholding_035` | 486 | cost of withholding a dimension or the large-n range | STALE | _none_ |
| `concl.metric_shift_006` | 486 | effect of removing the audited instances on every reported metric | UNVERIFIABLE | _none_ |

<details><summary>Notes and required actions</summary>

**`concl.feature_families`** — STRUCTURAL ⚑

> Two defects in one clause: the count moved 30 -> 31, and the 31st (greedy_nn_over_mst) is a greedy-tour ratio belonging to none of the three named families.

*Action:* See meth.feature_split_19 (FEATURE_FAMILIES) and meth.feature_count_166.

**`concl.sharing_vector`** — STRUCTURAL ⚑

> Sixth and final occurrence, in the sentence a reader is most likely to quote.

*Action:* See abs.identical_features.

**`concl.lowest_sdpe`** — BANKED

> Roster-conditional: fails on ND if GART 2.0 (V4 features) ships (0.9683 < 0.9881).

*Action:* See intro.superlative_all3 (SHIPPED_ROSTER).

**`concl.lowest_2d`** — BANKED

*Action:* See intro.superlative_all3.

**`concl.paired_zero`** — GENERATED

*Action:* See abs.tsplib_paired.

**`concl.linenoise`** — BANKED

> Says 11.59 where lines 364 and 428 say 11.60.

**`concl.slope_corr`** — UNGENERATED

*Action:* AUG-REPOINT plus the alpha-on-alpha exporter (aug.slope_corr).

**`concl.withholding_035`** — UNGENERATED

> Inconsistent with the 0.28 pp at line 400 for the same experiment.

*Action:* GEN-REPOINT.

**`concl.metric_shift_006`** — UNGENERATED

*Action:* See prov.other_estimators.

</details>

### Appendix (9)

| id | line | quantity | audit | authority / keys |
|---|---|---|---|---|
| `app.check_cell_count` | 498 | number of cells build_paper_tables --check verifies | UNVERIFIABLE | `paper_tooling/build_paper_tables.py --check` |
| `appx.out_of_interval_rows` | 499 | the two named corpus rows with raw alpha outside [1,2] | CORRECT | _none_ |
| `appx.feature_split` ⚑ | 566 | 19 MST + 11 geometric feature tables | STALE | `lgbm_model_v3/gart2_final.json`<br>`features_in_booster_order` |
| `appx.shap_count` ⚑ | 616 | tab:shap_top covers all features | STALE | `lgbm_model_v3/gart2_final.json`<br>`n_features` |
| `appx.shap_shares` | 618 | SHAP family shares | UNVERIFIABLE | _none_ |
| `appx.feature_set_620` ⚑ | 620 | feature count | STALE | `lgbm_model_v3/gart2_final.json`<br>`n_features` |
| `appx.nd_pair_sample` ⚑ ✓fixed | 830 | ND pair-sampling design | CORRECT | `paper_tooling/tables/paper_numbers.json`<br>`rank_nd_gart_2_0_pair_mode`<br>`rank_nd_gart_2_0_pair_seed`<br>`rank_nd_gart_2_0_close5_pairs` |
| `appx.geometric_11` | 1271 | 11 geometric and centroid features | CORRECT | `lgbm_model_v3/gart2_final.json`<br>`features_in_booster_order` |
| `appx.seven_explicit` | 1368 | GART MAPE on the EXPLICIT non-EUC_2D cases | STALE | `tsplib_nonEuc_explicit_gart_2_0_mape_pct`<br>`tsplib_nonEuc_explicit_gart_2_0_n` |

<details><summary>Notes and required actions</summary>

**`app.check_cell_count`** — GENERATED

> run_check() prints len(old) in its banner. The count moved with the resplice (tab:tsplib_nonEuc gained an N column, every results table gained a 'GART 2.0 (V3 features)' row), so it is stale but trivially recoverable. A self-describing count typed by hand into the document it describes is the same defect class in miniature.

*Action:* Have run_check() write tables/check_manifest.json {n_cells, n_moved, n_missing, generated_at} and splice n_cells.

**`appx.out_of_interval_rows`** — UNGENERATED

> Model-independent; the instance names and raw alphas were read off the corpus once by hand.

*Action:* Corpus-statistics exporter: bank the out-of-interval row names and their raw alpha.

**`appx.feature_split`** — STRUCTURAL ⚑

> greedy_nn_over_mst appears in neither appendix table.

*Action:* See meth.feature_split_19.

**`appx.shap_count`** — STRUCTURAL ⚑

> 30 rows for a 31-feature model.

*Action:* See meth.shap_shares.

**`appx.shap_shares`** — UNGENERATED

> Duplicate of meth.shap_shares, same numbers quoted twice.

*Action:* See meth.shap_shares.

**`appx.feature_set_620`** — STRUCTURAL ⚑

> Feature count in the feature-selection appendix, closing the loop back to Section 3.2.

*Action:* See meth.feature_count_166.

**`appx.nd_pair_sample`** — STRUCTURAL ⚑

> FLAGGED. The audit marked this CORRECT because the sample size is model-independent -- true, but the sampler no longer exists. The bank records pair_mode='exhaustive' and pair_seed='n/a'. A methods sentence describing a retired procedure is worse than a stale number, because no value check will ever fire on it.

**`appx.geometric_11`** — STRUCTURAL

> Correct for that table in isolation; only the document-level total is wrong.

**`appx.seven_explicit`** — BANKED

> TWO defects, and the second is the dangerous one. The value is stale (3.88 is the predecessor's), and the DENOMINATOR is wrong: the bank's N for that bucket is 6, not 7, because GART declines si1032. The manuscript reuses the taxonomy count (seven EXPLICIT instances, app.tsplib_taxonomy) as a scoring count. Whenever a model can decline an instance, a set size must be read from coverage, never from the taxonomy.

</details>

## Every UNGENERATED claim, grouped by the one thing that would fix it

- **GEN-REPOINT — repoint `generalization_experiments.py`** (10): `gen.held_out_sampling`, `gen.e0_bitforbit`, `gen.16846`, `gen.leave_dims_out`, `gen.neighbour_dims`, `gen.signed_error_d15`, `gen.leave_large_n_out`, `gen.within_range`, `gen.cost_028`, `concl.withholding_035`
- **One-off (see the claim's action)** (8): `meth.alpha_sd_by_d`, `meth.alphahat_range`, `meth.leaves_per_tree`, `prov.drop184_gart`, `prov.refit_clean`, `prov.other_estimators`, `tsplib.n_gt_10000`, `concl.metric_shift_006`
- **Corpus-statistics exporter** (6): `tsplib.alpha_moments`, `aug.alpha_gt_145`, `aug.corpus_rho_alpha`, `aug.geometry_facts`, `app.alpha_range_22`, `appx.out_of_interval_rows`
- **AUG-REPOINT — repoint the two augmentation experiments** (6): `aug.round1`, `aug.round2`, `aug.mean_pred_alpha`, `aug.half_linenoise`, `aug.grid_mspe`, `concl.slope_corr`
- **Oracle-constant exporter** (5): `abs.oracle_constant_78`, `tsplib.oracle_constant`, `tsplib.bound_margin`, `app.oracle_111`, `app.oracle_23`
- **Low-contention serial timing run** (4): `time.decomposition`, `time.median_vs_reference`, `time.bucket_501`, `time.d18512`
- **Booster-statistics exporter** (2): `meth.depth_stats`, `meth.comparisons`
- **SHAP re-run against the production booster** (2): `meth.shap_shares`, `appx.shap_shares`
- **alpha-on-alpha regression exporter** (1): `aug.slope_corr`

## Flagged: framed as invariant, moved anyway (⚑)

Every row here is a quantity the manuscript states as a fixed property of the work. Each one moved. The three causes are worth separating.

| id | line | quantity | why it moved |
|---|---|---|---|
| `abs.identical_features` | 90 | Linear and NN share GART's feature vector | Framed as a design invariant; it is not one |
| `intro.baseline_count` | 123 | size of the comparison roster | Framed as a fixed experimental design |
| `intro.identical_features` | 123 | Linear and NN share GART's feature vector | Duplicate of abs.identical_features. |
| `meth.feature_count_166` | 166 | feature count | One of six places the manuscript states the feature count as a design constant |
| `meth.feature_count_172` | 172 | feature count | Duplicate of meth.feature_count_166. |
| `meth.feature_split_19` | 188 | feature family decomposition 11 + 19 | 11 + 19 = 30 no longer adds up: greedy_nn_over_mst makes 31 and belongs to no named family |
| `meth.identical_212` | 212 | Linear and NN share GART's feature vector | Duplicate of abs.identical_features. |
| `meth.objective` | 214 | training objective | Framed as a method definition; the recipe changed under it |
| `meth.inference_clip` | 214 | existence of a post-hoc inference clip | clip_after_inverse=false |
| `meth.optuna` | 214 | hyperparameter provenance | NO MECHANICAL ROUTE |
| `meth.early_stopping` | 214 | early-stopping metric | Sidecar says cost_mape on the val split |
| `meth.complexity` | 219 | asymptotic cost of the feature pipeline | NO MECHANICAL ROUTE |
| `meth.shap_count` | 223 | feature count in the SHAP assessment | Feature count again, this time inside the SHAP paragraph, where it also sets the row count of tab:shap_top. |
| `bench.same30_238` | 238 | Linear and NN share GART's feature vector | Third of six occurrences of the same false parity statement. |
| `bench.same30_244` | 244 | Linear and NN share GART's feature vector | Fourth occurrence, and the one the benchmark-model table leans on. |
| `aug.success_criteria` | 406 | prespecified adoption thresholds for the augmentation | NO MECHANICAL ROUTE to the prespecification itself -- that a threshold was fixed in advance is a fact about the process, not a value |
| `disc.pairs_2d` | 420 | 2D close-pair universe at the 5% threshold | FLAGGED, and this one did NOT move with the model -- it moved with the GENERATOR |
| `disc.pairs_nd` | 420 | ND close-pair sampling design | FLAGGED |
| `app.two_of_thirty` | 436 | MDS pipeline omits 2 of the model's features | The denominator is the feature count, so it moved with it; the numerator (two omitted features) is a property of the MDS pipeline and did not. |
| `concl.feature_families` | 484 | feature count and family taxonomy | Two defects in one clause: the count moved 30 -> 31, and the 31st (greedy_nn_over_mst) is a greedy-tour ratio belonging to none of the three named families. |
| `concl.sharing_vector` | 484 | Linear and NN share GART's feature vector | Sixth and final occurrence, in the sentence a reader is most likely to quote. |
| `appx.feature_split` | 566 | 19 MST + 11 geometric feature tables | greedy_nn_over_mst appears in neither appendix table. |
| `appx.shap_count` | 616 | tab:shap_top covers all features | 30 rows for a 31-feature model. |
| `appx.feature_set_620` | 620 | feature count | Feature count in the feature-selection appendix, closing the loop back to Section 3.2. |
| `appx.nd_pair_sample` | 830 | ND pair-sampling design | FLAGGED |

## No mechanical route, even in principle (⛔)

This is the honest limit of what the paper can be made to check itself on. Everything else in this map can, with enough exporting, be diffed against an artifact. These cannot.

- **`meth.optuna`** (line 214, STRUCTURAL) — hyperparameter provenance
  - NO MECHANICAL ROUTE. The sidecar records 'V3's shipped values, frozen. Not tuned' as free text. A checker can assert the field is unchanged; it cannot verify a historical statement about how the values were obtained. Attestation, not measurement.
- **`meth.complexity`** (line 219, STRUCTURAL) — asymptotic cost of the feature pipeline
  - NO MECHANICAL ROUTE. An asymptotic statement cannot be settled by any artifact; it needs a human re-derivation against the inference module. It is on the drift list because its CONTENT moved with the model: the 31st feature adds a greedy-nearest-neighbour pass that the stated bounds do not account for. A timing curve would be corroboration, not proof.
- **`aug.success_criteria`** (line 406, STRUCTURAL) — prespecified adoption thresholds for the augmentation
  - NO MECHANICAL ROUTE to the prespecification itself -- that a threshold was fixed in advance is a fact about the process, not a value. FLAGGED because the thresholds are stated as absolute budgets ('<=0.05 pp ND regression') measured against a baseline that no longer ships, so a constant reads as having moved. Whether the criteria are MET is checkable once aug.slope_corr is banked.
- **`disc.boosting_conclusion`** (line 426, GENERATED) — attribution of the 2D advantage to features rather than boosting
  - NO MECHANICAL ROUTE to the claim itself: it is a causal attribution, and no artifact can certify 'the feature set is the source'. Its PREMISE (that the NN beats the boosted ensemble) is banked, and that premise is now false, so the sentence falls with it. A checker can flag the premise and must not be asked to judge the inference.

## Anchor verification

160 of 160 anchors located in the current `.tex`.

